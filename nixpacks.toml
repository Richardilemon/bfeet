[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["cd evangelism-heatmap && pip install -r requirements.txt"]

[start]
cmd = "cd evangelism-heatmap && uvicorn main:app --host 0.0.0.0 --port $PORT"

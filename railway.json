{
  "$schema": "https://schema.railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd evangelism-heatmap && pip3 install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd evangelism-heatmap && python3 -m uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}

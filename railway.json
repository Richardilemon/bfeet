{
  "$schema": "https://schema.railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "cd evangelism-heatmap && uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
